server:
  port: 80

eureka:
  client:
    #表示是否将自己注册将EurekaServer默认ture
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka

feign:
  hystrix:
    enabled: true
